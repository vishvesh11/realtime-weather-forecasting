apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "realtime-weather-app.chart-app.serviceAccountName" . }}
  labels:
    {{- include "realtime-weather-app.labels" . | nindent 4 }}
{{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
{{- end }}